#!/bin/bash

cd ~/docs || exit 1

if git diff --quiet && git diff --staged --quiet; then
    echo "No changes to commit"
    exit 0
fi

git add . || exit 1
git commit -m "backup" || exit 1
git push || exit 1